CCED Interactive Session (Fast)

- Results uses incremental Firestore updates (docChanges) for speed.
- WordCloud rendering is debounced + resize-safe.
- Fallback list appears if WordCloud library doesn't load.
- Participant submit has 8s timeout and doesn't hang on 'Submitting...'.
